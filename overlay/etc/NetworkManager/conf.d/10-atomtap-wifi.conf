[device]
# Use the real hardware MAC for Wi-Fi scanning so access points can see
# the device.  NM randomizes by default for privacy, which breaks setups
# where the AP filters or logs by MAC address.
wifi.scan-rand-mac-address=no

[connection]
# Use the permanent hardware MAC when associating (not a cloned/random one).
wifi.cloned-mac-address=permanent
